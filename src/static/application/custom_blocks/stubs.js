

// Block Definition - COMPLETE
Blockly.Blocks['wait'] = {
    init: function() {
      this.jsonInit(customBlocks.wait);
      var thisBlock = this;
      this.setTooltip(function() {
        return 'Tells the component to sleep for a specified number of seconds.';
      });
    }
};


Blockly.Blocks['set'] = {
    init: function() {
      this.jsonInit(customBlocks.set);
      var thisBlock = this;
      this.setTooltip(function() {
        return 'Tells the component to sleep for a specified number of seconds.';
      });
    }
};


Blockly.Blocks['add'] = {
    init: function() {
      this.jsonInit(customBlocks.add);
      var thisBlock = this;
      this.setTooltip(function() {
        return 'Tells the component to sleep for a specified number of seconds.';
      });
    }
};

  
Blockly.Blocks['speed'] = {
    init: function() {
      this.jsonInit(customBlocks.speed);
      var thisBlock = this;
      this.setTooltip(function() {
        return 'Tells the component to sleep for a specified number of seconds.';
      });
    }
};
  
  

  
Blockly.Blocks['power'] = {
    init: function() {
      this.jsonInit(customBlocks.power);
      var thisBlock = this;
      this.setTooltip(function() {
        return 'Tells the component to sleep for a specified number of seconds.';
      });
    }
};

  
Blockly.Blocks['brightness'] = {
    init: function() {
      this.jsonInit(customBlocks.brightness);
      var thisBlock = this;
      this.setTooltip(function() {
        return 'Tells the component to sleep for a specified number of seconds.';
      });
    }
};

  
Blockly.Blocks['bucket'] = {
    init: function() {
      this.jsonInit(customBlocks.bucket);
      var thisBlock = this;
      this.setTooltip(function() {
        return 'Tells the component to sleep for a specified number of seconds.';
      });
    }
};